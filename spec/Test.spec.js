describe("Always passing test", function() {
    it("should always pass", function() {
        // This test will always pass because it just contains a passing expectation
        expect(true).toBe(true);
    });
});